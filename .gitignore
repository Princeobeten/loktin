# stellar/soroban/Rust output
target
.soroban

# project identity and alias files will be in .config/stellar
.config

# local agent settings
.claude/

# test snapshot folders from sample contracts
test_snapshots

# build output
dist
dist-ssr

# dependencies
node_modules/

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# environment variables
.env
.env.*
!.env.example

# stellar CLI local identities / keys — never commit
.stellar/
.config/

# easy way to create temporary local-only files and folders
*.local.*

# macOS-specific files
.DS_Store

# generated contract clients — ignore build output and deps, but keep source
packages/*/node_modules
packages/*/dist

# generated contract client imports
src/contracts/*
!src/contracts/util.ts
!src/contracts/clientHelpers.ts

planning/
